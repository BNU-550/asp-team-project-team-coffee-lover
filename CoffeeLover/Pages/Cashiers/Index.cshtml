@page
@model CoffeeLover.Pages.Cashiers.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cashier[0].OrderItemsOrderID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cashier[0].OrderItemsProductsID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cashier[0].FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cashier[0].LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cashier[0].Email)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Cashier) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OrderItemsOrderID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrderItemsProductsID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.StaffID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.StaffID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.StaffID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
